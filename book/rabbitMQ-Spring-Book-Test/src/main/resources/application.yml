spring:
  application:
    name: rabbitMQ-Spring-Book-Test

  rabbitmq:
    host: 10.162.3.234
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: auto
        default-requeue-rejected: false
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 8111